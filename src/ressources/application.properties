# === SPRING DATASOURCE ===
spring.datasource.url=jdbc:postgresql://localhost:5432/recipeverse
spring.datasource.username=postgres
spring.datasource.password=your_password
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# === SERVER CONFIG ===
server.port=8080

# === JWT SECRET (même que dans JwtUtil.java) ===
jwt.secret=secret-key

# === MISTRAL AI CONFIG ===
mistral.api.url=https://api.mistral.ai/v1/chat/completions
mistral.api.key=Bearer mistral-xxxxxxxxxxxxxxxxxxxx
